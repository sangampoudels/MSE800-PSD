A decorator in Python is used to add extra functionality to a function without changing the original function code. Decorators help make programs cleaner, reusable, and easier to manage.

In our project, the decorator is used for activity logging and debugging.
g